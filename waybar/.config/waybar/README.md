# Must have **npm** or **yarn** installed. Inside the Waybar directory, run this:

```
yarn(or npm install)
```


# Style Waybar using Sass by just editing the src/style.sass. This will compile to style.css

```
yarn dev(or npm run dev)
```



